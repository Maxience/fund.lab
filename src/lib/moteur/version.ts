/**
 * Version du moteur de calcul, en numérotation sémantique.
 *
 * À incrémenter à chaque changement qui modifie un résultat : formule,
 * règle de décision, traitement des données manquantes ou arrondi. La
 * version de la méthodologie (coefficients) est portée séparément par la
 * configuration.
 */
export const VERSION_MOTEUR = '1.0.0';
