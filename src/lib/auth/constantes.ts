/**
 * Constantes d'authentification partagées entre le proxy (exécuté avant les
 * pages, sans accès à la base) et la couche serveur.
 */

export const NOM_COOKIE_SESSION = 'chalandise_session';

/** Préfixe des routes réservées aux Experts. */
export const PREFIXE_ESPACE_EXPERT = '/expert';

export const CHEMIN_CONNEXION = '/connexion';
