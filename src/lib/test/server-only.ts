/**
 * Remplacement du marqueur `server-only` pour les tests. Le paquet réel lève
 * une exception hors d'un composant serveur, ce qui empêcherait de tester la
 * couche service avec Vitest. Ce fichier est vide à dessein.
 */
export {};
