# Point d'avancement du 2026-09-07

## Réalisé

- Revue de l'état du projet face au brief : phases 0 à 5 confirmées terminées
  (moteur, parcours PME, données, authentification, espace Expert, 179
  tests). Reste à traiter : phase 6 (qualité transversale) et phase 7
  (déploiement et livraison), non commencées.
- Ouverture d'une checklist de livraison vivante
  (`docs/avancement/checklist-livraison.md`), tenue à jour à chaque tâche
  terminée pour cadrer les derniers jours.
- Page d'accueil simplifiée : titre, description et bouton d'action centrés,
  le reste du contenu retiré.
- Nettoyage du dépôt avant la dernière ligne droite : `.gitignore` complété
  (fichiers générés par `next dev`, brief confidentiel FUND.lab,
  configuration personnelle de l'assistant), changements en attente
  committés, `npm run verifier` au vert.
- Contrôle qu'aucun secret n'est présent dans l'historique git ni dans le
  code.

- Barre latérale de navigation dans l'espace Expert, en-têtes fixes au
  défilement sur l'accueil, la connexion et le parcours en huit étapes.
- Compte Expert supplémentaire créé et vérifié de bout en bout
  (`npm run auth:creer`, `npm run auth:verifier`).
- Base de test dédiée `chalandise_test`, distincte du développement et de la
  démonstration, avec sa propre suite Vitest (`npm run test:recette`,
  `docs/decisions/0006-tests-de-recette-base-dediee.md`). Sept tests de
  recette écrits et verts : R01, R02, R03, R04, R05, R08, R09. R10 déjà
  couvert par le moteur ; R06 et R07 par `npm run auth:verifier` ; R11
  vérifié par export PDF manuel ; R12 reste procédural. Q-04 de la checklist
  est donc coché.
- Matrice de traçabilité recalée sur l'audit sécurité et l'inventaire des
  dépendances ajoutés en parallèle.

- Phase 6 terminée. Q-01 (responsive à 360 px, protocole DevTools pour
  contourner une limite de largeur de l'outil de capture habituel), Q-02
  (revue d'accessibilité : labels, clavier, focus visible, couleur jamais
  seule) et Q-03 (performance mesurée sur une vraie construction de
  production, un point de vigilance documenté sur la latence réseau vers la
  base de développement distante). Preuves dans `docs/preuves/`.

- Documentation technique (L6, `docs/documentation-technique.md`) et
  documentation utilisateur (L7, `docs/utilisateur/guide-utilisateur.md`
  avec six captures réelles), registre des écarts (L9,
  `docs/registre-ecarts.md`) et feuille de route (L10,
  `docs/feuille-de-route.md`).
- Lancement à froid rejoué (cas R12) : dépôt vidé de `node_modules`,
  `.next` et du client Prisma généré, réinstallation et démarrage complets
  en 7,5 minutes sans intervention hors des étapes documentées.
- `npm audit` relevé 4 alertes de sévérité haute lors de la réinstallation,
  toutes portées par des dépendances de la CLI Prisma (jamais présentes
  dans la construction de production) : constat documenté dans
  `docs/inventaire-dependances.md`, aucune correction de code nécessaire.
- Phase 7 : cinq tâches sur sept terminées (L-02, L-03, L-04, L-05, et R12
  qui débloque une partie de L-06). Restent L-01 (déploiement, en attente
  de comptes d'hébergement), une partie de L-06 (recette sur l'URL
  déployée) et L-07 (démonstration et transfert des accès).

## Prochain objectif

- Envoyer le message de questions à FUND.lab
  (`docs/cadrage/questions-fundlab.md`), toujours en attente.
- L-01 : déploiement sur une URL de recette, dès que les comptes
  d'hébergement (Vercel et Neon proposés) sont créés.
- L-06 et L-07 une fois l'URL de recette disponible.

## Blocage

- Aucun nouveau blocage technique. Le calendrier du brief (sept jours
  ouvrés) est serré au vu de ce qu'il reste en phase 6 et 7 : à traiter en
  priorité dès aujourd'hui.
